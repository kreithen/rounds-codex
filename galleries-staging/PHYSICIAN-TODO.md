# What only Dr. Kreithen can do — mislabelled-anatomy project

Running list. Everything here is blocked on you, not on me. Ordered by what unblocks the most work.
Nothing on this list is launch-blocking.

*Last updated 2026-08-10.*

---

## 1. Approve the 81-page work order — this is the big one

`Rounds-Codex-leader-line-production-order.docx` — **81 pages, 272 labels to move, 235 to check,
across 75 galleries.** Approval is **by page**, so it is 81 decisions rather than 511. Production
cannot start re-rendering until pages are signed.

You do not have to do them all at once. **Send me whichever sheets you have signed and I will send
those to production**; the brief itself (`PRODUCTION-BRIEF-leader-lines.md`) has already gone.

The rows marked **[CHECK]** are the auditors' SUSPECT calls — they were told to flag rather than
guess, so a fair number are correct as drawn. Those specifically need your eye.

## 2. Seven production sheets are sent and waiting on production, not on you

Listed so you know what is already out of the queue. No action needed unless production comes back
with questions.

| sheet | what it asks for |
|---|---|
| `PRODUCTION-BRIEF-leader-lines.md` | the two template faults causing most of the 553 findings |
| `PRODUCTION-H7-sensation-map-redraw.md` | MSK H7 — the deep peroneal nerve map redrawn |
| `PRODUCTION-pericarditis-p4-ECG.md` | the ECG strip redrawn with PR depression actually present |
| `PRODUCTION-aortic-stenosis-p2.md` | heart anatomy page |
| `PRODUCTION-acs-p6.md` | the magnifier ring |
| `PRODUCTION-cardiomyopathy-p1.md` | three of four phenotype callouts land in the wrong slab |
| `PRODUCTION-hyperlipidemia-p5-p6.md` | two figures that cannot show what they are labelled with |

## 3. Two Higgsfield generations were never retrieved

Two `cardiomyopathy` variants were generated and the retrieval call needed an approval that never
came through, so I have never seen them. **They are in your Higgsfield gallery** — a glance will
tell you whether they are worth anything. If they look wrong in the way the previous four did, say
so and I will write them off; the route is otherwise closed for this project (0-for-4 on marker
moves, recorded in `HANDOFF-anatomy-label-corrections.md`).

## 4. A decision I would like from you eventually — the 27 sub-standard galleries

`addisons` is 800×1200; the standard is 1024×1536. The current best guess is that these pages were
*delivered* at the standard size and downscaled by our own old pipeline, which would mean the fix is
**re-pasting from your photo album**, not a production re-export. That is one gallery's worth of
pasting to find out. Say when you want to pilot it — it is not urgent and it is separate from the
label work.

---

## Not on this list

Things I am doing myself and will not ask you about: the local leader/dot/marker corrections, their
QA, the per-batch deploys, and the production sheets for anything that turns out not to be locally
fixable. Each deploy gets its own note in `artwork-fixes/PENDING-DEPLOY.md` with what changed and
what was measured, so any of them can be reversed if you disagree with the call.
