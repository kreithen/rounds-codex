# HANDOFF — the mislabelled anatomy project

Written 2026-08-09 to move this work into its own conversation. **Read this before touching
anything.** It is the bridge between what the build sessions already established and what is left.

`CLAUDE.md` at the repo root is still the authority on the project as a whole; this file is the
authority on *this* project.

---

## What the project is

Gallery anatomy pages carry labels joined to the artwork by leader lines. **On a large fraction of
pages the line does not end on the structure the label names.** That teaches the wrong anatomy, and
it is the defect Dr. Kreithen reported.

Measured across the pages checked so far:

| | |
|---|---|
| pages examined | **119** |
| **wrong** — a line lands on the wrong structure | **81** |
| suspect — could not be called either way | 17 |
| clean | 21 |
| individual label findings | **553** |

The earlier subset that went into the production brief put it at **323 of 1,098 labels — 30%.**

---

## The state of every artifact

Everything below is in `galleries-staging/` and already committed.

| file | what it is | state |
|---|---|---|
| `LABEL-QA.md` | The full machine-readable finding list, 136 kB, generated by `scripts/qa_label_report.py`. Sorted WRONG / SUSPECT / CLEAN. | **Candidate list, not a verdict.** Every line needs the physician before it goes to production. |
| `PRODUCTION-BRIEF-leader-lines.md` | Explains the two mechanical **causes**, not the symptoms. Short, meant to be sent first. | **Ready to send. Not sent.** |
| `Rounds-Codex-leader-line-corrections.docx` | The audit, for the record. | Built. |
| `Rounds-Codex-leader-line-production-order.docx` | The work order — 81 pages, one per sheet, an Approve/Amend box on each. | **Built, awaiting the physician's approval.** |
| `HIGGSFIELD-CORRECTION-PILOT.md` | Proof that AI can fix a mislabelled panel. | **Answered: yes.** See below. |
| `ARTWORK-CORRECTIONS.md` | Earlier artwork correction log. | Background. |
| `PRODUCTION-CORRECTIONS-MSK.md` | The two new MSK galleries. Mostly template faults, but **H7 is an anatomy-label defect and belongs to this project**. | Sent as a document; H1 and H3 already fixed in-app (v81). |
| `DOTS-defect-for-production.md` | Header progress-dot defects. **Adjacent, not this project.** | Not sent. |
| `label-qa-agent-prompt.md` | The prompt used to drive the QA agents. | Reusable. |

### The decision already taken

**Brief first, then the work order.** The physician chose to send
`PRODUCTION-BRIEF-leader-lines.md` ahead of the page list, so production fixes the *cause* rather
than working through 81 symptoms. Neither has been sent yet.

---

## Three rules that are not negotiable

1. **NEVER repaint delivered artwork.** A dot-row repainter was written once. It detected the row by
   autocorrelation, sampled colours off the page, rebuilt the background by interpolation, and
   refused pages it could not read. It still produced half-erased "U"-shaped dots, skipped 31 of 80
   pages as unreadable, and was deleted. **Flag for re-render instead.**
   *The narrow exception, already used:* `scripts/transplant_gallery_text.py` and
   `scripts/fix_gallery_icd.py` copy glyphs that **already exist elsewhere in the same batch** at the
   same size and font onto a background measured flat. Nothing is detected, invented or
   reconstructed. Read their docstrings before using them, and do not widen what they do.
2. **The physician is the medical gate.** `LABEL-QA.md` is a candidate list. The agents were told to
   report SUSPECT rather than guess, so the SUSPECT section is *expected* to contain correct pages.
   Nothing goes to production as a confirmed defect without his sign-off.
3. **Look at the image.** Every finding in this project came from reading a page by eye. The
   measurement failures below all came from trusting a number instead.

---

## Higgsfield: the alternative to a production re-render

`HIGGSFIELD-CORRECTION-PILOT.md` answers the physician's question — **yes, a mislabelled panel can be
AI-corrected**, at 18 credits over three runs on `pericarditis` p2. Two things make it work:

- **Crop out just the panel.** Run 1 sent the whole page and it regenerated the entire page.
- **One change per run.**

The physician's stated preference is to **regenerate only the incorrect panel and photoshop it into
the page himself.** An `aortic-dissection` anatomy panel was regenerated on that basis and he said he
would photoshop it when back at his desktop — **check whether that came back before starting new
generations.**

**Higgsfield spends the physician's credits. Do not run a batch without explicit approval past a
validated pilot.** The connector is also frequently unavailable; see the connector notes in
`CLAUDE.md`.

---

## What is actually left

1. **Get the work order approved.** 81 pages, Approve/Amend per page. It is the physician's read, not
   a task that can be automated.
2. **Send the brief, then the approved order** to the artwork vendor.
3. **Decide per page: production re-render, or Higgsfield panel + photoshop.** The pilot makes the
   second real; the first scales better.
4. **MSK H7** — `hip-fracture` page 5 attributes the whole dorsum of the foot to the deep peroneal
   nerve, which supplies only the first web space; superficial peroneal is missing from the legend
   entirely. **Check whether the map's colour regions need redrawing too, not just the text.**
5. **Rebuild any gallery whose pages change** — `scripts/build_galleries_from_images.py`, then
   **`scripts/rebuild_gallery_pdf.py`**, because the download PDF is a build artifact and drifts
   otherwise. That is exactly how four galleries shipped a stale logo. Then
   `scripts/verify_gallery_pdfs.py` as the standing guard.

---

## Measurement traps, all paid for already

- **A dot count read off a downscaled image is wrong.** Three findings were retracted this way in one
  day — `hip-fracture` pages 1, 3 and 6, plus `back-pain` page 5. Two of those retractions took a
  headline conclusion with them.
- **Full-resolution pages are already on disk.** The physician pastes them from his photo album into
  chat and they land in `/root/.claude/uploads/<session-id>/` at **1024×1536**. An entire session was
  lost asking for files that were already there. Use `scripts/ingest_pasted_gallery.py`.
- **Control before you conclude.** A PDF logo comparison gave three confident wrong answers (91, then
  88, then 4 galleries affected) before it gave the right one. Control for size, then position, then
  look by eye.
- **A guard that passes on the broken build is decoration.** Run any new checker against the pre-fix
  file and confirm it fails.

---

## Branch discipline — this matters

The main build conversation works on **`claude/usmle-rounds-codex-module-bmpl61`**. Two conversations
pushing there will collide, and `legal/README.md` already records that lesson.

**Use `claude/anatomy-label-corrections`** for this project. Branch it from the same base:

```sh
git fetch origin && git checkout -b claude/anatomy-label-corrections origin/claude/usmle-rounds-codex-module-bmpl61
```

**Do not deploy to `rounds-codex-app` from this conversation without saying so first.** The web launch
is targeted at **17 August** and the other conversation is shipping to `main`; an unexpected deploy
from here lands in the middle of that. See `LAUNCH-AUG17.md`.

---

## Where this sits relative to the launch

**It is not launch-blocking.** `LAUNCH-AUG17.md` §4.1 lists the anatomy corrections as production's
work and explicitly excludes them from the 17 August critical path. The galleries ship as they are;
corrected pages drop in afterwards with no app change beyond a rebuild.

---

## MSK H7 — resolved to a specification (2026-08-10)

`hip-fracture` p5 was read at full resolution and the H7 note's premise was **wrong**: it assumed a
dorsal image needing a relabel. **The map is a PLANTAR (sole) view**, so deep peroneal — which
supplies no plantar skin at all — cannot appear on it, and two of the three structures the exam
bullets ask the reader to test are dorsal and unshowable on a sole. The full region-by-region
finding is in `PRODUCTION-CORRECTIONS-MSK.md` §H7a.

**A dorsal reference map was generated and the physician approved it** (Higgsfield `nano_banana_2`,
2k, from a hand-drawn territory layout — the model is not trusted to place nerve territories itself).
QA'd numerically before he saw it, which is the fast and stronger check:

| zone | share of foot | centroid (x: 0=medial, 1=lateral) |
|---|---|---|
| superficial peroneal (pink) | 68.1% | 0.54 — broad dorsum |
| deep peroneal (purple) | **2.8%** | 0.36, y=0.23 — 1st web space |
| saphenous (green) | 14.4% | **0.15** — medial border |
| sural (blue) | 10.1% | **0.86** — lateral border |
| near-white pixels (text/labels) | **0** | — |

Approved render: `hf_20260810_030924_49f5cb83-0edc-4cc4-b292-45bf3a971e46.png` in the Higgsfield
gallery. **It is a specification, not final artwork** — production re-renders in the gallery's style.

**Route chosen (physician, 2026-08-10): fold into the production re-render batch**, not composited
here. Sent as a standalone sheet, `PRODUCTION-H7-sensation-map-redraw.md`, because
`PRODUCTION-CORRECTIONS-MSK.md` **had already gone to production carrying the superseded H7** — an
item that asks for a relabel, which on a plantar map would leave the page wrong. The old item is
banner-marked SUPERSEDED in place. **When a sent document's finding changes materially, reissue it
rather than editing quietly** — the vendor is working from the copy they already hold.

**It cannot be dropped into the page as-is, and that is the important part.** The correction also
changes *text*: the legend gains a fifth entry (Superficial peroneal) and the three exam bullets are
reworded. Inventing those glyphs on delivered artwork is exactly what rule 1 forbids, and the
`transplant_gallery_text.py` exception does not cover it — that tool only copies glyphs that already
exist elsewhere in the same batch, and "Superficial peroneal" appears nowhere. So H7 is a
**full-page re-render**, either by production or by the physician in Photoshop.

### A SECOND defect found on the same page (2026-08-10)

The physician then flagged the **pulse figure** in the same panel. It is a dorsal view whose
**"Posterior tibial pulse" leader ends on the dorsum over the metatarsals** — the PT pulse is
palpated behind and below the *medial malleolus*, so there is **no correct endpoint for that label on
a dorsal figure at all**. Same failure mode as the sensation map: the label names something the view
cannot show.

Clinically it is the worst item on the page. DP and PT are checked as a pair *because they test
different arterial supplies*; someone taught to palpate "posterior tibial" on the dorsum is palpating
the anterior circulation twice, so an occluded posterior tibial artery would examine as normal.

Also on that figure: the great toe and lesser toes are shaded **purple**, the legend's deep-peroneal
colour, repeating the same wrong teaching. Flagged with the caveat that purple may be the source
atlas's own dorsal-digital-nerve convention — but on a page whose legend defines purple as deep
peroneal, it cannot be read that way. The DP leader itself is *acceptable* (slightly proximal of the
standard point) and was explicitly not called an error.

**The pattern worth carrying forward: check whether a label names a structure the chosen VIEW can
show at all, before checking whether the leader landed on it.** Both defects on this page are of
that kind, and neither is detectable by asking "does the line end on the right structure" — for both,
no right structure is present in the frame.

### The class generalises, and the sweep's coverage is narrower than its headline

Mined the existing `label-qa-raw/` findings for the same signature: the class is **already in the
data**, it was just never named. Confirmed twins of the `hip-fracture` p5 defect on `compartment` p2
(flexor retinaculum, deep posterior compartment and tibial nerve all placed on an anterior view that
cannot show them) and `anxiety` p2 (insula on a midsagittal cut). `compartment` p2 also **recolours
the artwork to match the wrong label**, exactly as the purple toes do — two galleries, one mistake,
same mechanism, so it is a source-artwork pattern rather than a slip.

**The bigger finding is the coverage gap.** The sweep is effectively a *page-2 sweep* — 90 of 109
worklist entries are page 2, 15 are page 1 — against ~1,000 pages that exist. So "119 examined, 81
wrong, 553 findings" describes the **anatomy pages**, not the galleries. The working assumption was
that labelled anatomy lives on pages 1-2; `hip-fracture` p5 is titled *Physical Exam* and carries two
labelled anatomical figures, both defective, which disproves it. The 81-page work order is therefore
not a clean bill of health for the other ~880 pages, and a re-render batch fixing the 81 would not
have caught this page.

Full write-up, with the mining precision (23 broad candidates, ~4 true — a review aid, **not** a
gate) and a bounded sampling proposal, in **`VIEW-MISMATCH-class.md`**.

### Two workflow lessons, both cost real time this session

- **Do not round-trip generated images to my own eyes.** The container is firewalled from the result
  CDN, so viewing one means smuggling base64 through `sandbox_exec` in ~20 kB chunks — slow, and the
  first attempt came back too compressed to judge. **Hand the physician the CDN URL**; it is public
  and opens full-resolution in his browser. His eye is the gate regardless, so my look is a redundant
  second pass.
- **QA numerically instead.** A colour-zone probe run in the sandbox returns a text report in one
  call and proves the assertions that actually matter (is deep peroneal small, is it in the web
  space, are medial/lateral swapped, is there burned-in text). It is stronger evidence than a visual
  read *and* far faster. What it cannot judge — toe count, proportions, whether the render reads as
  dorsal — is exactly what to ask the physician to confirm.
